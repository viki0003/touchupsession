 {/* Conditional rendering of time inputs */}
                                            {/* {appointment?.sessions[sessionIndex]?.date && (
                                                <div className="two-column-layout">

                                                    <div className="form-group">
                                                        <label htmlFor="start data">Start Time</label>
                                                        <input
                                                            // type="date"
                                                            // id="start-date"
                                                            // name="start-date"
                                                            // value={formData.phone}
                                                            // onChange={handleChange}

                                                            type="time"
                                                            id={`start-time-${index}-${sessionIndex}`}
                                                            name={`start-time-${index}-${sessionIndex}`}
                                                            onChange={(e) =>
                                                                handleSessionDateChange(index, sessionIndex, 'startTime', e.target.value)
                                                            }

                                                            required
                                                        />
                                                    </div>
                                                    <div className="form-group">
                                                        <label htmlFor="start data">End Time</label>
                                                        <input
                                                            // type="date"
                                                            // id="start-date"
                                                            // name="start-date"
                                                            // value={formData.phone}
                                                            // onChange={handleChange}
                                                            type="time"
                                                            id={`end-time-${index}-${sessionIndex}`}
                                                            name={`end-time-${index}-${sessionIndex}`}
                                                            onChange={(e) =>
                                                                handleSessionDateChange(index, sessionIndex, 'endTime', e.target.value)
                                                            }
                                                            required
                                                        />
                                                    </div>
                                                </div>
                                            )} */}                   
                   
                   
                   
                   
                   
                   
                   
                   
                   
                    <div className="two-column-layout">
                        <div className="form-group">
                            <label htmlFor="title">Title</label>
                            <input
                                type="text"
                                id="title"
                                name="title"
                                value={formData.email}
                                onChange={handleChange}
                                required
                            />
                        </div>
                        <div className="form-group">
                            <label htmlFor="start data">Start Date</label>
                            <input
                                // type="date"
                                id="start-date"
                                name="start-date"
                                value={formData.phone}
                                onChange={handleChange}
                                required
                            />
                        </div>
                    </div>

                    <button>Get Available slot</button>



                    <div className="two-column-layout">
                        <div className="form-group">
                            <label htmlFor="previous_tattoos">Select No. of Sessions *</label>
                            <select
                                id="no_of_sessions"
                                name="no_of_sessions"
                                value={formData.previous_tattoos}
                                onChange={handleChange}
                                required
                            >
                                <option value="" disabled>Select an option</option>
                                <option value="1">1</option>
                                <option value="2">2</option>
                                <option value="3">3</option>
                                <option value="4">4</option>
                                <option value="5">5</option>
                                <option value="6">6</option>
                                <option value="7">7</option>
                                <option value="8">8</option>
                                <option value="9">9</option>
                                <option value="10">10</option>
                                <option value="11">11</option>
                                <option value="12">12</option>
                                <option value="13">13</option>
                                <option value="14">14</option>
                                <option value="15">15</option>
                                <option value="16">16</option>
                            </select>
                        </div>
                        <div className="form-group">
                            <label htmlFor="start data">Select Date</label>
                            <input
                                type="date"
                                id="selected_ate"
                                name="selected_date"
                                value={formData.phone}
                                onChange={handleChange}
                                required
                            />
                        </div>
                    </div>


                    <button>Create Touchup Session</button>
